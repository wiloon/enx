# ENX Data Service Configuration

node:
  id: "macbook"
  grpc_port: 50051  # gRPC service port
  http_port: 8090   # HTTP API port

# Peer nodes to sync with
peers:
  - addr: "192.168.50.19:50051"
    name: "ubuntu-laptop"
