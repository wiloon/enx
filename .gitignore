# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go build outputs
enx-api/enx-api
enx-api/enx-server
enx-api/migrate

# Dependency directories (remove the comment below to include it)
# vendor/
.idea/
chrome-enx/node_modules
chrome-enx/public
.vscode
enx-chrome/node_modules
enx-chrome/dist
enx-api/build
mock-api/node_modules

# npm lock file (we use pnpm)
package-lock.json

# Air live reload temporary files
tmp/
build-errors.log

# Coverage reports
coverage.out
coverage.html
